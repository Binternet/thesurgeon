apiVersion: v1
kind: Service
metadata:
  name: {{ include "hcomp-app.name" . }}
  labels:
    {{- include "hcomp-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hcomp-app.selectorLabels" . | nindent 4 }}
