# ArgoCD Application for hcomp-app — Production EU (prd-eu-cluster).
# Same repo as other envs; this one tracks main. Register cluster: argocd cluster add prd-eu-cluster
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: hcomp-app-prd-eu
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/Binternet/thesurgeon.git
    targetRevision: main
    path: charts/hcomp-app
    helm:
      valueFiles:
        - values.yaml
        - values-prd-eu.yaml
  destination:
    name: prd-eu-cluster
    namespace: hcomp-app
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
